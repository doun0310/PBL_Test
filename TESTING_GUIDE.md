# Diet Tracker 앱 테스트 가이드

## 빌드 및 실행

### 1. 환경 준비

```bash
# Flutter SDK가 설치되어 있는지 확인
flutter --version

# 프로젝트 디렉토리로 이동
cd /path/to/PBL_Test

# 의존성 설치
flutter pub get
```

### 2. 디바이스 확인

```bash
# 사용 가능한 디바이스 확인
flutter devices

# 출력 예시:
# Android emulator (mobile) • emulator-5554
# iPhone 14 Pro Max (mobile) • ios-simulator
```

### 3. 앱 실행

```bash
# 기본 디바이스에서 실행
flutter run

# 특정 디바이스 지정
flutter run -d emulator-5554

# 릴리즈 모드로 실행
flutter run --release
```

## 기능별 테스트 체크리스트

### ✅ 메인 대시보드

- [ ] 앱 시작 시 대시보드가 정상적으로 로드되는가?
- [ ] 영양 진행 카드가 표시되는가?
  - [ ] 칼로리 진행바
  - [ ] 탄수화물, 단백질, 지방 원형 차트
- [ ] 날짜 선택기가 작동하는가?
  - [ ] 좌측 화살표 (이전 날짜)
  - [ ] 캘린더 아이콘 (날짜 선택 다이얼로그)
  - [ ] 우측 화살표 (다음 날짜)
- [ ] 빈 상태 메시지가 표시되는가?
- [ ] 하단 네비게이션 바가 작동하는가?
  - [ ] 홈 탭
  - [ ] 추가 탭
  - [ ] 통계 탭
  - [ ] 프로필 탭

### ✅ 식사 추가

#### 식사 유형 선택
- [ ] 아침, 점심, 저녁, 간식 탭이 작동하는가?
- [ ] 선택된 탭의 스타일이 변경되는가?

#### 사진으로 음식 분석
- [ ] 카메라 버튼 클릭 시 카메라가 열리는가?
- [ ] 갤러리 버튼 클릭 시 갤러리가 열리는가?
- [ ] 사진 선택 후 분석 화면으로 이동하는가?
- [ ] 분석 로딩 애니메이션이 표시되는가? (2초)
- [ ] 음식이 검출되어 표시되는가?
- [ ] 각 음식의 양 슬라이더가 작동하는가?
- [ ] 영양 정보가 실시간으로 업데이트되는가?
- [ ] 음식 삭제 버튼이 작동하는가?
- [ ] 확인 버튼으로 음식이 추가되는가?

#### 영양성분표 스캔
- [ ] "곧 추가됩니다" 메시지가 표시되는가?

#### 음식 검색
- [ ] 검색창이 작동하는가?
- [ ] 실시간 검색 필터링이 되는가?
- [ ] 카테고리별 음식 표시가 되는가?
- [ ] 음식 선택 시 체크 표시가 되는가?
- [ ] 선택된 음식 칩이 상단에 표시되는가?
- [ ] 완료 버튼으로 음식이 추가되는가?

#### 식사 저장
- [ ] 선택한 음식 목록이 표시되는가?
- [ ] 각 음식의 양 슬라이더가 작동하는가?
- [ ] 영양 정보가 정확히 표시되는가?
- [ ] 저장 버튼이 작동하는가?
- [ ] 저장 후 대시보드로 돌아가는가?
- [ ] 저장된 식사가 대시보드에 표시되는가?

### ✅ 식사 추천

- [ ] 추천 버튼이 대시보드에 표시되는가?
- [ ] 남은 칼로리가 정확히 계산되는가?
- [ ] 칼로리 슬라이더가 작동하는가? (100-1000)
- [ ] 슬라이더 조절 시 추천이 업데이트되는가?
- [ ] 추천 카드가 표시되는가? (최대 8개)
- [ ] 각 추천의 영양 정보가 표시되는가?
- [ ] 추천 클릭 시 상세 정보 모달이 열리는가?
- [ ] 식사 추가 버튼이 작동하는가?

### ✅ 통계 및 분석

#### 기간 선택
- [ ] 주간/월간 토글이 작동하는가?
- [ ] 선택에 따라 데이터가 업데이트되는가?

#### 영양소 그래프
- [ ] 차트가 정상적으로 렌더링되는가?
- [ ] 탄수화물, 단백질, 지방 막대가 표시되는가?
- [ ] 색상 범례가 표시되는가?
- [ ] X축 레이블이 표시되는가? (주간: 요일, 월간: 날짜)
- [ ] 빈 데이터 시 메시지가 표시되는가?

#### 음식 랭킹
- [ ] 가장 많이 먹은 음식 순위가 표시되는가?
- [ ] 1-3위에 메달이 표시되는가? (🥇🥈🥉)
- [ ] 섭취 횟수가 표시되는가?
- [ ] 칼로리 정보가 표시되는가?
- [ ] 빈 데이터 시 메시지가 표시되는가?

### ✅ 프로필 및 목표 설정

- [ ] 프로필 카드가 표시되는가?
- [ ] 목표 슬라이더들이 작동하는가?
  - [ ] 칼로리 (1000-3000)
  - [ ] 탄수화물 (50-400)
  - [ ] 단백질 (30-150)
  - [ ] 지방 (30-100)
- [ ] 슬라이더 값이 실시간으로 표시되는가?
- [ ] 저장 버튼이 작동하는가?
- [ ] 저장 후 스낵바 메시지가 표시되는가?
- [ ] 저장된 값이 대시보드에 반영되는가?

### ✅ 데이터 지속성

- [ ] 앱 종료 후 재시작 시 데이터가 유지되는가?
  - [ ] 식사 기록
  - [ ] 사용자 목표
- [ ] 날짜별로 데이터가 정확히 저장되는가?

### ✅ UI/UX

- [ ] 모든 화면이 스크롤 가능한가?
- [ ] 터치 반응이 즉각적인가?
- [ ] 색상이 일관성 있게 적용되는가?
- [ ] 그라데이션 효과가 표시되는가?
- [ ] 섀도우가 적절히 표시되는가?
- [ ] 아이콘이 명확하게 보이는가?
- [ ] 텍스트가 읽기 쉬운가?
- [ ] 버튼 크기가 적절한가?
- [ ] 로딩 인디케이터가 표시되는가?

## 성능 테스트

### 메모리 및 성능
```bash
# 성능 오버레이 표시
flutter run --profile

# DevTools로 성능 분석
flutter pub global activate devtools
flutter pub global run devtools
```

### 체크 항목
- [ ] 스크롤이 부드러운가? (60 FPS)
- [ ] 화면 전환이 매끄러운가?
- [ ] 메모리 누수가 없는가?
- [ ] 앱 크기가 적절한가?

## 빌드 테스트

### Android APK
```bash
# Debug APK
flutter build apk --debug

# Release APK
flutter build apk --release

# APK 위치: build/app/outputs/flutter-apk/
```

### iOS IPA (macOS에서만)
```bash
# Release IPA
flutter build ios --release

# IPA 위치: build/ios/iphoneos/
```

## 오류 발생 시 확인 사항

### 의존성 오류
```bash
flutter clean
flutter pub get
```

### 빌드 오류
```bash
# Gradle 캐시 삭제 (Android)
cd android
./gradlew clean
cd ..

# Pods 재설치 (iOS)
cd ios
rm -rf Pods Podfile.lock
pod install
cd ..
```

### 권한 오류

#### Android
`android/app/src/main/AndroidManifest.xml`에 추가:
```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
```

#### iOS
`ios/Runner/Info.plist`에 추가:
```xml
<key>NSCameraUsageDescription</key>
<string>음식 사진을 촬영하려면 카메라 접근이 필요합니다.</string>
<key>NSPhotoLibraryUsageDescription</key>
<string>음식 사진을 선택하려면 갤러리 접근이 필요합니다.</string>
```

## 알려진 제한사항

1. **AI 음식 인식:** 실제 ML 모델이 아닌 시뮬레이션 (랜덤 음식 3개)
2. **OCR 스캔:** UI만 구현, 실제 기능은 미구현
3. **클라우드 동기화:** 로컬 저장소만 사용
4. **사용자 인증:** 없음 (단일 사용자)

## 버그 리포팅

버그 발견 시 다음 정보를 포함하여 GitHub Issues에 제출:

1. 디바이스 정보 (기종, OS 버전)
2. Flutter 버전
3. 재현 단계
4. 기대 동작
5. 실제 동작
6. 스크린샷 (선택사항)
7. 에러 로그 (선택사항)

## 다음 단계

모든 테스트가 통과하면:

1. 릴리즈 빌드 생성
2. 앱 스토어 메타데이터 준비
3. 스크린샷 촬영
4. 개인정보 처리방침 작성 (필요시)
5. 앱 스토어 제출

---

**참고:** 이 테스트 가이드는 주요 기능에 대한 수동 테스트를 위한 것입니다. 자동화된 테스트는 별도로 작성해야 합니다.
