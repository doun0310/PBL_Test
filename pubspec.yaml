name: meal_management_app
description: 학교 식단 관리 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP 요청
  http: ^1.1.0

  # 로컬 저장소 (JWT 토큰 저장)
  shared_preferences: ^2.2.2

  # 날짜 포맷팅
  intl: ^0.20.2

  # 아이콘
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
